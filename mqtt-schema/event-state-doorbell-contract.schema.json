{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/jbruns/smartchime/docs/event-state-doorbell-contract.schema.json",
  "title": "Smartchime Doorbell Event Contract",
  "description": "Doorbell event published from Home Assistant to Smartchime via MQTT (topic: smartchime/events/doorbell). Triggers chime playback when active.",
  "type": "object",
  "required": ["active", "timestamp"],
  "additionalProperties": false,
  "properties": {
    "active": {
      "type": "boolean",
      "description": "Whether the doorbell is currently ringing. true triggers chime playback."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of the event. Naive timestamps are assumed UTC."
    }
  }
}
